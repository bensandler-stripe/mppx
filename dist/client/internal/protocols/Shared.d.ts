/** HTTP status that signals a native payment challenge. */
export declare const paymentRequiredStatus = 402;
/** Attaches `credential` under `header`, clearing any stale credential headers first. */
export declare function setCredentialHeader(request: RequestInit, header: string, credential: string): RequestInit;
//# sourceMappingURL=Shared.d.ts.map